def revers_word(string):
    # my_list = string.split()
    # my_reversed_lsit = my_list[::-1]
    # my_reversed_text = ' '.join(my_reversed_lsit)


    return ' '.join( string.split()[::-1])   #this same code but this shortcut for all that's lines

print(revers_word("hi mohamed and how are you today"))
